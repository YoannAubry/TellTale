services:
  db:
    image: postgres:16
    container_name: telltale-db
    environment:
      POSTGRES_DB: telltale
      POSTGRES_USER: captain
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - ./data-db:/var/lib/postgresql/data
